/**
 * @format
 */

import { AppRegistry } from 'react-native';
import GameScene from './source/GameScene';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => GameScene);
